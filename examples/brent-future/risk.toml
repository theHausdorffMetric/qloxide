# Risk config for the brent-future book (arch §9): a single future, so
# no vol_data — the risk report is the outright delta + model book value.
instruments = ["instruments.json"]
indices = ["indices.json"]
deals = ["deals.json"]
market = "market.json"
reports = ["risk"]
