# Risk config for the example-public book (arch §9): the same data
# files as book.toml, plus the risk-tier vol history.
instruments = ["instruments.json"]
indices = ["indices.json"]
deals = ["deals.json"]
market = "market.json"
vol_data = ["vols.json"]
reports = ["risk"]
