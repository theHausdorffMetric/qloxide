# Risk config for the brent-option book (arch §9): the same data files
# as book.toml, plus the risk-tier vol history. Consumed by qloxide-risk
# only.
instruments = ["instruments.json"]
indices = ["indices.json"]
deals = ["deals.json"]
market = "market.json"
vol_data = ["vols.json"]
reports = ["risk"]
