# Brent Dec26 72 call, bought 2026-07-01 @ 5.57 (15 lots; ATM: Z26
# settled 72.16 that day): a real book against real ICE EOD settlement
# data, and — being a single long-dated tenor (option expiry
# 2026-10-27) — the second RND test case beside brent-condor's short
# Sep26 tenor.
#
# market.json is the one-file market history (arch §9): every trading
# day from deal inception to the generation point, generated by
# qloxide-ice from ICE cleared settlements. Book data is vol-free —
# the risk-tier surfaces live in vols.json, wired via risk.toml.
#
# Price from this directory (see the justfile here):
#   just book             (all reports, valued at the last history day)
#   just day 2026-07-01   (what-if: current book vs one history day)
#   just series           (P&L across all history days)
instruments = ["instruments.json"]
indices = ["indices.json"]
deals = ["deals.json"]
market = "market.json"
reports = ["instruments", "deals", "positions", "pnl"]
