# Risk config for the brent-options prototype (arch §9): the same data
# files as book.toml, plus the risk-tier vol history. Consumed by
# qloxide-risk only; scenario overlays come in via --market.
instruments = ["instruments.json"]
indices = ["indices.json"]
deals = ["deals.json"]
market = "market.json"
vol_data = ["vols.json"]
reports = ["risk"]
